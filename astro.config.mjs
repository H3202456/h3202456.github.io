import { defineConfig } from "astro/config";

export default defineConfig({
    site: "https://arifmolla13446.github.io",

    // 如果你的仓库名不是 arifmolla13446.github.io，
    // 而是 ai-demo-portfolio，就改成这样：
    // base: "/ai-demo-portfolio",
});